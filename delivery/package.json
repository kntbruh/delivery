{
  "name": "delivery",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev -w @delivery/web",
    "dev:web": "npm run dev -w @delivery/web",
    "build": "npm run build -w @delivery/web",
    "lint": "npm run lint -w @delivery/web"
  },
  "devDependencies": {
    "turbo": "^2.5.4"
  }
}
